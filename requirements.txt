psutil  # optional — enables CPU/RAM monitoring via Python instead of WMI
websocket-client  # optional — real OBS status (OBS WebSocket) AND near-instant
                  # channel-point redemptions + raid/bits/sub hype via Twitch EventSub.
                  # Without it, redemptions still work via polling (a few seconds' latency).
