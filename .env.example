# Twitch API credentials
# Get these from https://dev.twitch.tv/console/apps
#   - Register an application, OAuth redirect URL: http://localhost
#   - Under "Client Type", choose Public (enables the Device Code login the
#     interactive features use — no secret is sent for that flow).
# The SAME app powers both the read-only status polling and the interactive
# layer (chat + channel points). No extra credentials are needed; the first
# time you run with interactive features on, you'll authorize your account via
# a short code (Twitch Device Code Flow) and the token is cached locally in
# .twitch_user_token.json (gitignored).
TWITCH_CLIENT_ID=your_client_id_here
TWITCH_CLIENT_SECRET=your_client_secret_here

# OBS WebSocket (optional) — enables real scene/stream/record status instead
# of just "is obs64.exe running". In OBS: Tools > WebSocket Server Settings >
# Enable WebSocket server, then copy the password shown there.
# Requires: pip install websocket-client
# OBS_WEBSOCKET_PASSWORD=your_obs_websocket_password
# OBS_WEBSOCKET_HOST=localhost
# OBS_WEBSOCKET_PORT=4455
